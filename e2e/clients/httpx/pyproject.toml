[project]
name = "bankofai.x402-httpx-e2e"
version = "0.1.0"
description = "Python httpx client for x402 e2e tests"
requires-python = ">=3.10"
dependencies = [
    "python-dotenv>=1.0.0",
    "bankofai.x402[httpx,evm,svm,tron,extensions]"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]

[tool.hatch.metadata]
allow-direct-references = true

[tool.uv]
package = false

[tool.uv.sources]
"bankofai.x402" = { path = "../../../python/x402", editable = true }
