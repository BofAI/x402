# Key custody is handled by @bankofai/agent-wallet — this example never reads a
# raw key. agent-wallet's EnvWalletProvider resolves the signing wallet from its
# own env vars (or a keystore/Privy if you configure a provider dir). Fill the
# chain(s) you want enabled; the other self-skips.

# EVM wallet (agent-wallet, network "evm"):
AGENT_WALLET_PRIVATE_KEY=

# TRON wallet (agent-wallet, network "tron"):
TRON_PRIVATE_KEY=

# Chain RPC — reads/broadcast only, NOT key custody:
EVM_RPC_URL=
TRON_GRID_API_KEY=

PORT=4022
