/**
 * Wallet module — removed in favor of agent-wallet package.
 *
 * Signing is now handled directly by agent-wallet's EvmWallet / TronWallet.
 * Signers accept a private key string or an agent-wallet BaseWallet instance.
 */
