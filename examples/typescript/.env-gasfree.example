# ── Scenario: GasFree (scheme `exact_gasfree`, TRON-only) ──────────────────
# Copy to `.env-gasfree` (gitignored); the gasfree trio loads it:
#   clients/gasfree · servers/gasfree · facilitator/gasfree
#
#   cp .env-gasfree.example .env-gasfree
#
# TRON-only: the payer signs a TIP-712 GasFree permit and a relayer pays the
# on-chain energy. Funds come from the payer's GasFree custodial wallet (not the
# main wallet), which must be activated and funded with USDT on Nile.
#
# One file, three processes. Vars grouped by the process that reads them:
# SHARED (client + facilitator), CLIENT (payer), SERVER (resource provider),
# FACILITATOR (settlement / relayer).

# ── SHARED · client + facilitator ─────────────────────────────────────────
# Signing key — payer (client) and settler (facilitator).
AGENT_WALLET_PRIVATE_KEY=0x...

# TronGrid API key — both client and facilitator touch TRON. Optional.
TRON_GRID_API_KEY=

# GasFree relayer API. The client resolves the service provider against it and
# the facilitator submits through it. Optional — defaults to the built-in Nile
# relayer.
# GASFREE_API_URL=

# ── CLIENT · the payer (clients/gasfree) ──────────────────────────────────
# The resource the client fetches (server route).
RESOURCE_URL=http://localhost:4031/weather

# ── SERVER · the resource provider (servers/gasfree) ──────────────────────
# Payout address (TRON only).
TRON_ADDRESS=T...   # TRON, tron:0xcd8690dc

# Where the server binds, and where it reaches the facilitator.
SERVER_PORT=4031
FACILITATOR_URL=http://localhost:4032

# Optional: drop `paymentPayload.resource` (a localhost URL when the server runs
# locally) before verify/settle. Set "true" only when a LOCAL server points at a
# hosted facilitator whose edge WAF rejects loopback URLs in the body (e.g. AWS
# `EC2MetaDataSSRF_BODY` -> 403). Facilitator never reads it; signature ignores it.
# STRIP_RESOURCE_URL=true

# Optional: facilitator API key, sent as the `X-API-KEY` header on every
# verify/settle/supported call the SERVER makes. Hosted facilitators use it to
# select a rate-limit tier — anonymous calls still work, so leave it unset for
# local runs.
# FACILITATOR_API_KEY=your-key-here

# ── FACILITATOR · settlement (facilitator/gasfree) ────────────────────────
# Where the facilitator binds (FACILITATOR_URL above must point here).
FACILITATOR_PORT=4032
